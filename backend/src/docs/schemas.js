/**
 * Reusable OpenAPI components — schemas and security schemes referenced via
 * $ref from every route's JSDoc. Adding a new domain schema here (and
 * exporting it) makes it available to any future module's route
 * annotations without touching swagger.js.
 */
export const components = {
  securitySchemes: {
    bearerAuth: {
      type: 'http',
      scheme: 'bearer',
      bearerFormat: 'JWT',
      description:
        'Firebase ID token, sent as "Authorization: Bearer <token>". Verified via Firebase Admin (see modules/auth/identityProvider.port.js), then reconciled to an internal user via Identity Sync — req.user carries the Postgres user, never the raw Firebase identity.',
    },
  },
  schemas: {
    ApiResponse: {
      type: 'object',
      description: 'Standard success envelope every endpoint returns.',
      properties: {
        success: { type: 'boolean', example: true },
        message: { type: 'string', example: 'Success' },
        data: { nullable: true },
        meta: { $ref: '#/components/schemas/Pagination' },
      },
    },
    ErrorResponse: {
      type: 'object',
      description: 'Standard error envelope every failed request returns.',
      properties: {
        success: { type: 'boolean', example: false },
        message: { type: 'string', example: 'Something went wrong' },
      },
    },
    ValidationError: {
      description: 'Returned when request body/query/params fail Zod validation (HTTP 422).',
      allOf: [
        { $ref: '#/components/schemas/ErrorResponse' },
        {
          type: 'object',
          properties: {
            details: {
              type: 'object',
              additionalProperties: { type: 'array', items: { type: 'string' } },
              example: { businessName: ['Required'] },
            },
          },
        },
      ],
    },
    NotFoundError: {
      description: 'Returned when the requested resource does not exist (HTTP 404).',
      allOf: [
        { $ref: '#/components/schemas/ErrorResponse' },
        {
          type: 'object',
          properties: { message: { type: 'string', example: 'Merchant not found' } },
        },
      ],
    },
    Pagination: {
      type: 'object',
      properties: {
        page: { type: 'integer', example: 1 },
        pageSize: { type: 'integer', example: 20 },
        total: { type: 'integer', example: 42 },
        totalPages: { type: 'integer', example: 3 },
      },
    },
    Merchant: {
      type: 'object',
      properties: {
        id: { type: 'string', format: 'uuid' },
        businessName: { type: 'string', example: 'Iron Forge Fitness' },
        legalName: { type: 'string', nullable: true, example: 'Iron Forge Fitness Pvt Ltd' },
        businessType: {
          type: 'string',
          enum: [
            'gym',
            'meal-provider',
            'wellness-center',
            'yoga-studio',
            'physio-clinic',
            'nutrition-center',
            'fitness-chain',
            'other',
          ],
        },
        status: { type: 'string', enum: ['pending', 'active', 'suspended'] },
        contactEmail: { type: 'string', format: 'email', nullable: true },
        contactPhone: { type: 'string', nullable: true },
        address: { type: 'string', nullable: true },
        timezone: { type: 'string', nullable: true, example: 'Asia/Kolkata' },
        currency: { type: 'string', example: 'INR', description: 'ISO 4217 code.' },
        country: { type: 'string', nullable: true, example: 'India' },
        createdBy: { type: 'string', format: 'uuid', nullable: true },
        updatedBy: { type: 'string', format: 'uuid', nullable: true },
        createdAt: { type: 'string', format: 'date-time' },
        updatedAt: { type: 'string', format: 'date-time' },
      },
    },
    Branch: {
      type: 'object',
      properties: {
        id: { type: 'string', format: 'uuid' },
        merchantId: { type: 'string', format: 'uuid' },
        name: { type: 'string', example: 'Anna Nagar' },
        address: { type: 'string', nullable: true },
        city: { type: 'string', example: 'Chennai' },
        state: { type: 'string', nullable: true, example: 'Tamil Nadu' },
        country: { type: 'string', nullable: true, example: 'India' },
        postalCode: { type: 'string', nullable: true, example: '600040' },
        phoneCode: { type: 'string', nullable: true, example: '46', description: 'Required by Surfboard\'s Create Store API (Phase 2).' },
        phoneNumber: { type: 'string', nullable: true, example: '701234567', description: 'Required by Surfboard\'s Create Store API (Phase 2).' },
        status: { type: 'string', enum: ['active', 'inactive'] },
        createdBy: { type: 'string', format: 'uuid', nullable: true },
        updatedBy: { type: 'string', format: 'uuid', nullable: true },
        createdAt: { type: 'string', format: 'date-time' },
        updatedAt: { type: 'string', format: 'date-time' },
      },
    },
    Device: {
      type: 'object',
      properties: {
        id: { type: 'string', format: 'uuid' },
        branchId: { type: 'string', format: 'uuid' },
        label: { type: 'string', example: 'Front Desk Terminal' },
        status: { type: 'string', enum: ['registered', 'active', 'offline', 'deactivated'] },
        brandingConfig: { type: 'object', additionalProperties: true, nullable: true },
        tipConfig: { type: 'object', additionalProperties: true, nullable: true },
        registrationIdentifier: { type: 'string', nullable: true, description: 'Required by Surfboard\'s Register Terminal API (Phase 2) — the 6-digit code shown on power-on, or serial number for SurfPad/Printer.' },
        createdBy: { type: 'string', format: 'uuid', nullable: true },
        updatedBy: { type: 'string', format: 'uuid', nullable: true },
        createdAt: { type: 'string', format: 'date-time' },
        updatedAt: { type: 'string', format: 'date-time' },
      },
    },
    MembershipPlan: {
      type: 'object',
      properties: {
        id: { type: 'string', format: 'uuid' },
        merchantId: { type: 'string', format: 'uuid' },
        name: { type: 'string', example: 'Muscle Gain Package' },
        description: { type: 'string', nullable: true },
        price: { type: 'string', example: '2499.00', description: 'Decimal string — never a float, to avoid rounding error.' },
        currency: { type: 'string', example: 'INR' },
        billingCycle: { type: 'string', enum: ['one-time', 'monthly', 'quarterly', 'yearly'] },
        status: { type: 'string', enum: ['active', 'archived'] },
        createdBy: { type: 'string', format: 'uuid', nullable: true },
        updatedBy: { type: 'string', format: 'uuid', nullable: true },
        createdAt: { type: 'string', format: 'date-time' },
        updatedAt: { type: 'string', format: 'date-time' },
      },
    },
    Subscription: {
      type: 'object',
      properties: {
        id: { type: 'string', format: 'uuid' },
        membershipPlanId: { type: 'string', format: 'uuid' },
        customerId: { type: 'string', format: 'uuid' },
        status: { type: 'string', enum: ['active', 'cancelled', 'expired'] },
        startedAt: { type: 'string', format: 'date-time' },
        cancelledAt: { type: 'string', format: 'date-time', nullable: true },
        createdAt: { type: 'string', format: 'date-time' },
        updatedAt: { type: 'string', format: 'date-time' },
      },
    },
    PlatformDashboardSummary: {
      type: 'object',
      description: 'Platform-wide counts plus a fixed set of small "latest N" operational summaries (Phase 2) for the Platform Control Center dashboard. Every count is a single aggregated SQL scalar, not a per-row scan; every list below is independently bounded (LIMIT 5).',
      properties: {
        merchants: { type: 'integer', example: 42 },
        activeMerchants: { type: 'integer', example: 30 },
        pendingMerchants: { type: 'integer', example: 8, description: 'Count, not a list — see pendingMerchantsList for the list.' },
        suspendedMerchants: { type: 'integer', example: 4, description: 'Count, not a list — see suspendedMerchantsList for the list.' },
        branches: { type: 'integer', example: 120 },
        devices: { type: 'integer', example: 340 },
        membershipPlans: { type: 'integer', example: 95 },
        subscriptions: { type: 'integer', example: 610 },
        platformAdmins: { type: 'integer', example: 3, description: 'Distinct users holding the platform-admin role.' },
        merchantOwners: { type: 'integer', example: 42, description: 'Distinct users holding merchant-owner at any merchant.' },
        merchantStaff: { type: 'integer', example: 85, description: 'Distinct users holding merchant-staff at any merchant.' },
        viewers: { type: 'integer', example: 20, description: 'Distinct users holding viewer at any merchant.' },
        latestMerchants: {
          type: 'array',
          items: {
            type: 'object',
            properties: {
              id: { type: 'string', format: 'uuid' },
              businessName: { type: 'string' },
              status: { type: 'string', enum: ['pending', 'active', 'suspended'] },
              createdAt: { type: 'string', format: 'date-time' },
            },
          },
        },
        latestUsers: {
          type: 'array',
          items: {
            type: 'object',
            properties: {
              id: { type: 'string', format: 'uuid' },
              displayName: { type: 'string', nullable: true },
              email: { type: 'string', format: 'email' },
              status: { type: 'string', enum: ['active', 'invited', 'disabled'] },
              createdAt: { type: 'string', format: 'date-time' },
            },
          },
        },
        latestDevices: {
          type: 'array',
          items: {
            type: 'object',
            properties: {
              id: { type: 'string', format: 'uuid' },
              branchId: { type: 'string', format: 'uuid' },
              label: { type: 'string' },
              status: { type: 'string', enum: ['registered', 'active', 'offline', 'deactivated'] },
              createdAt: { type: 'string', format: 'date-time' },
            },
          },
        },
        latestMembershipPlans: {
          type: 'array',
          items: {
            type: 'object',
            properties: {
              id: { type: 'string', format: 'uuid' },
              merchantId: { type: 'string', format: 'uuid' },
              name: { type: 'string' },
              status: { type: 'string', enum: ['active', 'archived'] },
              createdAt: { type: 'string', format: 'date-time' },
            },
          },
        },
        latestSubscriptions: {
          type: 'array',
          items: {
            type: 'object',
            properties: {
              id: { type: 'string', format: 'uuid' },
              membershipPlanId: { type: 'string', format: 'uuid' },
              status: { type: 'string', enum: ['active', 'cancelled', 'expired'] },
              startedAt: { type: 'string', format: 'date-time' },
              createdAt: { type: 'string', format: 'date-time' },
            },
          },
        },
        pendingMerchantsList: {
          type: 'array',
          items: {
            type: 'object',
            properties: {
              id: { type: 'string', format: 'uuid' },
              businessName: { type: 'string' },
              createdAt: { type: 'string', format: 'date-time' },
            },
          },
        },
        suspendedMerchantsList: {
          type: 'array',
          items: {
            type: 'object',
            properties: {
              id: { type: 'string', format: 'uuid' },
              businessName: { type: 'string' },
              createdAt: { type: 'string', format: 'date-time' },
            },
          },
        },
        newestMerchantStaff: {
          type: 'array',
          items: {
            type: 'object',
            properties: {
              id: { type: 'string', format: 'uuid' },
              userId: { type: 'string', format: 'uuid' },
              displayName: { type: 'string', nullable: true },
              email: { type: 'string', format: 'email' },
              merchantId: { type: 'string', format: 'uuid' },
              businessName: { type: 'string' },
              roleName: { type: 'string' },
              status: { type: 'string', enum: ['active', 'removed'] },
              createdAt: { type: 'string', format: 'date-time' },
            },
          },
        },
        newestPlatformAdmins: {
          type: 'array',
          items: {
            type: 'object',
            properties: {
              userId: { type: 'string', format: 'uuid' },
              displayName: { type: 'string', nullable: true },
              email: { type: 'string', format: 'email' },
              createdAt: { type: 'string', format: 'date-time' },
            },
          },
        },
        recentActivity: {
          type: 'array',
          description: 'A lightweight preview of the most recent audit_logs entries — see /platform/audit for the full explorer.',
          items: {
            type: 'object',
            properties: {
              id: { type: 'string', format: 'uuid' },
              entityType: { type: 'string' },
              entityId: { type: 'string', format: 'uuid' },
              action: { type: 'string' },
              actor: {
                type: 'object',
                nullable: true,
                properties: { id: { type: 'string', format: 'uuid' }, displayName: { type: 'string', nullable: true } },
              },
              timestamp: { type: 'string', format: 'date-time' },
            },
          },
        },
      },
    },
    PlatformHealth: {
      type: 'object',
      description: 'Live checks, computed fresh on every call — no monitoring history is stored.',
      properties: {
        api: { type: 'object', properties: { status: { type: 'string', example: 'ok' } } },
        database: {
          type: 'object',
          properties: {
            status: { type: 'string', enum: ['ok', 'error'] },
            responseTimeMs: { type: 'integer' },
            message: { type: 'string', nullable: true },
          },
        },
        firebase: {
          type: 'object',
          properties: {
            status: { type: 'string', enum: ['ok', 'not_configured', 'error'] },
            responseTimeMs: { type: 'integer' },
            message: { type: 'string', nullable: true },
          },
        },
        surfboard: {
          type: 'object',
          description:
            'Reports whether SURFBOARD_BASE_URL/PARTNER_ID/API_KEY/API_SECRET are all set (see surfboard.health.js). Authentication is a confirmed static API-KEY/API-SECRET header pair, not a token exchange — there is no confirmed side-effect-free Surfboard endpoint to verify live connectivity against yet, so `authenticated` is `null` (not verified) whenever `configured` is true, rather than a guessed true/false.',
          properties: {
            status: { type: 'string', enum: ['configured', 'not_configured'] },
            configured: { type: 'boolean' },
            authenticated: { type: 'boolean', nullable: true, description: 'Always null today — see description.' },
            message: { type: 'string', nullable: true },
            responseTimeMs: { type: 'integer' },
          },
        },
        queue: {
          type: 'object',
          description: 'Placeholder — no queue worker/consumer exists yet.',
          properties: { status: { type: 'string', example: 'not_implemented' }, message: { type: 'string' } },
        },
        storage: {
          type: 'object',
          description: 'Placeholder — no storage service is configured yet.',
          properties: { status: { type: 'string', example: 'not_implemented' }, message: { type: 'string' } },
        },
        uptimeSeconds: { type: 'integer', example: 3600 },
        version: { type: 'string', example: '0.0.0' },
        environment: { type: 'string', example: 'development' },
        responseTimeMs: { type: 'integer', example: 12 },
      },
    },
    PlatformAuditEntry: {
      type: 'object',
      properties: {
        id: { type: 'string', format: 'uuid' },
        timestamp: { type: 'string', format: 'date-time' },
        actor: {
          type: 'object',
          nullable: true,
          properties: {
            id: { type: 'string', format: 'uuid' },
            displayName: { type: 'string', nullable: true },
            email: { type: 'string', format: 'email' },
          },
        },
        entity: {
          type: 'object',
          properties: { type: { type: 'string', example: 'merchant' }, id: { type: 'string', format: 'uuid' } },
        },
        action: { type: 'string', example: 'merchant.created' },
        metadata: { type: 'object', additionalProperties: true, nullable: true },
        before: { type: 'object', nullable: true, description: 'Always null — the current audit write path never captures a pre-change snapshot.' },
        after: { type: 'object', nullable: true, description: 'The same value as metadata.' },
      },
    },
    Incident: {
      type: 'object',
      properties: {
        id: { type: 'string', format: 'uuid' },
        title: { type: 'string' },
        description: { type: 'string', nullable: true },
        severity: { type: 'string', enum: ['low', 'medium', 'high', 'critical'] },
        status: { type: 'string', enum: ['open', 'investigating', 'resolved', 'closed', 'cancelled'] },
        category: { type: 'string', nullable: true },
        merchantId: { type: 'string', format: 'uuid', nullable: true },
        branchId: { type: 'string', format: 'uuid', nullable: true },
        deviceId: { type: 'string', format: 'uuid', nullable: true },
        createdBy: { type: 'string', format: 'uuid', nullable: true },
        assignedTo: { type: 'string', format: 'uuid', nullable: true },
        createdAt: { type: 'string', format: 'date-time' },
        updatedAt: { type: 'string', format: 'date-time' },
      },
    },
    Runbook: {
      type: 'object',
      properties: {
        id: { type: 'string', format: 'uuid' },
        title: { type: 'string' },
        category: { type: 'string', nullable: true },
        description: { type: 'string', nullable: true },
        steps: { type: 'array', items: {}, description: 'Free-form ordered procedure content.' },
        relatedIncidentTypes: { type: 'array', items: { type: 'string' } },
        version: { type: 'integer', example: 1, description: 'Auto-incremented on every update — not client-settable.' },
        active: { type: 'boolean' },
        createdBy: { type: 'string', format: 'uuid', nullable: true },
        updatedBy: { type: 'string', format: 'uuid', nullable: true },
        createdAt: { type: 'string', format: 'date-time' },
        updatedAt: { type: 'string', format: 'date-time' },
      },
    },
    Recommendation: {
      type: 'object',
      description: 'The Incident -> Recommendation -> Runbook link.',
      properties: {
        id: { type: 'string', format: 'uuid' },
        incidentId: { type: 'string', format: 'uuid' },
        runbookId: { type: 'string', format: 'uuid' },
        description: { type: 'string', nullable: true },
        status: { type: 'string', enum: ['suggested', 'applied', 'dismissed'] },
        createdBy: { type: 'string', format: 'uuid', nullable: true },
        createdAt: { type: 'string', format: 'date-time' },
        updatedAt: { type: 'string', format: 'date-time' },
      },
    },
    PlatformMerchantOverviewItem: {
      type: 'object',
      properties: {
        merchant: {
          type: 'object',
          properties: {
            id: { type: 'string', format: 'uuid' },
            businessName: { type: 'string', example: 'Iron Forge Fitness' },
          },
        },
        status: { type: 'string', enum: ['pending', 'active', 'suspended'], description: 'The real, mutable lifecycle/access-control field — set by Activate/Deactivate/Edit.' },
        onboardingStage: {
          type: 'string',
          enum: ['draft', 'invited', 'active', 'suspended'],
          description: 'Read-only, computed onboarding-progress indicator layered on top of `status` — "draft" (pending, no owner invited yet) and "invited" (pending, owner invitation outstanding) both collapse to `status: "pending"`; "active"/"suspended" mirror `status` directly. Not a separate persisted field.',
        },
        branchCount: { type: 'integer', example: 3 },
        deviceCount: { type: 'integer', example: 12 },
        staffCount: { type: 'integer', example: 5, description: 'Active merchant_staff assignments only.' },
        membershipPlanCount: { type: 'integer', example: 4 },
        createdAt: { type: 'string', format: 'date-time' },
      },
    },
    PlatformMerchantStaffEntry: {
      type: 'object',
      properties: {
        id: { type: 'string', format: 'uuid' },
        userId: { type: 'string', format: 'uuid' },
        displayName: { type: 'string', nullable: true },
        email: { type: 'string', format: 'email' },
        roleName: { type: 'string', example: 'merchant-owner' },
        status: { type: 'string', enum: ['active', 'removed'] },
        createdAt: { type: 'string', format: 'date-time' },
      },
    },
    PlatformMerchantDetails: {
      type: 'object',
      properties: {
        merchant: { $ref: '#/components/schemas/Merchant' },
        onboardingStage: {
          type: 'string',
          enum: ['draft', 'invited', 'active', 'suspended'],
          description: 'Read-only, computed onboarding-progress indicator — see PlatformMerchantOverviewItem.onboardingStage.',
        },
        branches: { type: 'array', items: { $ref: '#/components/schemas/Branch' } },
        devices: { type: 'array', items: { $ref: '#/components/schemas/Device' } },
        membershipPlans: { type: 'array', items: { $ref: '#/components/schemas/MembershipPlan' } },
        merchantStaff: {
          type: 'array',
          items: { $ref: '#/components/schemas/PlatformMerchantStaffEntry' },
          description: 'Includes historical (removed) assignments, not just currently-active staff.',
        },
      },
    },
    PlatformUserOverviewItem: {
      type: 'object',
      properties: {
        id: { type: 'string', format: 'uuid' },
        displayName: { type: 'string', nullable: true },
        email: { type: 'string', format: 'email' },
        status: { type: 'string', enum: ['active', 'invited', 'disabled'] },
        roles: {
          type: 'array',
          items: { type: 'string' },
          example: ['merchant-owner', 'merchant-staff'],
          description: 'Union of global (user_roles) and active merchant-scoped (merchant_staff) role names.',
        },
        merchantCount: { type: 'integer', example: 2, description: 'Distinct merchants this user is actively staffed at.' },
      },
    },
    PlatformMerchantAssignment: {
      type: 'object',
      properties: {
        merchantId: { type: 'string', format: 'uuid' },
        businessName: { type: 'string', example: 'Iron Forge Fitness' },
        roleName: { type: 'string', example: 'merchant-owner' },
        status: { type: 'string', enum: ['active', 'removed'] },
        createdAt: { type: 'string', format: 'date-time' },
      },
    },
    PlatformUserDetails: {
      type: 'object',
      properties: {
        user: {
          type: 'object',
          properties: {
            id: { type: 'string', format: 'uuid' },
            firebaseUid: { type: 'string' },
            email: { type: 'string', format: 'email' },
            displayName: { type: 'string', nullable: true },
            status: { type: 'string', enum: ['active', 'invited', 'disabled'] },
            createdAt: { type: 'string', format: 'date-time' },
            updatedAt: { type: 'string', format: 'date-time' },
          },
        },
        merchantAssignments: {
          type: 'array',
          items: { $ref: '#/components/schemas/PlatformMerchantAssignment' },
          description: 'Every merchant this user has ever been staffed at, including historical (removed) assignments.',
        },
        roles: { type: 'array', items: { type: 'string' }, example: ['merchant-owner'] },
        permissions: {
          type: 'array',
          items: { type: 'string' },
          example: ['merchant.read', 'merchant.write', 'branch.read'],
          description: 'Union of every permission granted by any of this user\'s assigned roles.',
        },
      },
    },
    MerchantOnboardingRequest: {
      type: 'object',
      required: ['merchant'],
      properties: {
        merchant: {
          type: 'object',
          required: ['businessName', 'businessType'],
          properties: {
            businessName: { type: 'string', example: 'Iron Forge Fitness' },
            legalName: { type: 'string', nullable: true, example: 'Iron Forge Fitness Pvt Ltd' },
            businessType: {
              type: 'string',
              enum: ['gym', 'meal-provider', 'wellness-center', 'yoga-studio', 'physio-clinic', 'nutrition-center', 'fitness-chain', 'other'],
            },
            contactEmail: { type: 'string', format: 'email', nullable: true },
            contactPhone: { type: 'string', nullable: true },
            address: { type: 'string', nullable: true },
            timezone: { type: 'string', nullable: true, example: 'Asia/Kolkata' },
            currency: { type: 'string', example: 'INR' },
            country: { type: 'string', nullable: true, example: 'India' },
          },
          description: 'No `status` field — a merchant is always born "pending" (see merchant.service.js\'s existing business rule); this endpoint does not accept one.',
        },
        ownerEmail: {
          type: 'string',
          format: 'email',
          description: 'Optional — omit to create the merchant alone and invite its owner later via POST /platform/invitations.',
        },
        ownerDisplayName: { type: 'string', nullable: true },
      },
    },
    MerchantOnboardingResult: {
      type: 'object',
      properties: {
        merchant: { $ref: '#/components/schemas/Merchant' },
        ownerLinked: { type: 'boolean', description: 'true if ownerEmail matched an existing user (linked immediately); false if a pending invitation was created instead.' },
        staffAssignment: {
          type: 'object',
          nullable: true,
          properties: {
            id: { type: 'string', format: 'uuid' },
            merchantId: { type: 'string', format: 'uuid' },
            userId: { type: 'string', format: 'uuid' },
            roleId: { type: 'string', format: 'uuid' },
            status: { type: 'string', example: 'active' },
          },
          description: 'Present only when ownerLinked is true.',
        },
        invitation: {
          allOf: [{ $ref: '#/components/schemas/MerchantInvitationWithToken' }],
          nullable: true,
          description: 'Present only when ownerLinked is false.',
        },
      },
    },
    MerchantInvitation: {
      type: 'object',
      description: 'Never includes the token or its hash — see MerchantInvitationWithToken for the one response that does.',
      properties: {
        id: { type: 'string', format: 'uuid' },
        merchantId: { type: 'string', format: 'uuid' },
        businessName: { type: 'string' },
        email: { type: 'string', format: 'email' },
        displayName: { type: 'string', nullable: true },
        roleId: { type: 'string', format: 'uuid' },
        role: { type: 'string', enum: ['merchant-owner', 'merchant-staff', 'viewer'] },
        status: { type: 'string', enum: ['pending', 'accepted', 'expired', 'revoked'] },
        expiresAt: { type: 'string', format: 'date-time' },
        acceptedAt: { type: 'string', format: 'date-time', nullable: true },
        createdBy: { type: 'string', format: 'uuid', nullable: true },
        createdAt: { type: 'string', format: 'date-time' },
        updatedAt: { type: 'string', format: 'date-time' },
      },
    },
    MerchantInvitationWithToken: {
      description: 'Only returned once, from create/resend — the plaintext token is never stored and can never be retrieved again after this response.',
      allOf: [
        { $ref: '#/components/schemas/MerchantInvitation' },
        { type: 'object', properties: { token: { type: 'string', example: 'b6f1...64 hex chars' } } },
      ],
    },
    MerchantInvitationPreview: {
      type: 'object',
      description: 'GET /auth/invitations/preview\'s response — deliberately narrower than MerchantInvitation (no id/roleId/timestamps): just enough for the acceptance UI to show what the invitee is being asked to join, before they commit.',
      properties: {
        merchantId: { type: 'string', format: 'uuid' },
        businessName: { type: 'string', example: 'Iron Forge Fitness' },
        email: { type: 'string', format: 'email' },
        role: { type: 'string', enum: ['merchant-owner', 'merchant-staff', 'viewer'] },
        status: { type: 'string', enum: ['pending', 'accepted', 'expired', 'revoked'] },
        expiresAt: { type: 'string', format: 'date-time' },
      },
    },
    MerchantSyncResult: {
      type: 'object',
      description: 'Result of one sync attempt (POST .../sync). `status` reflects the real outcome — today that is always "failed", since the Surfboard adapter is a Sprint 2A stub (see docs/architecture/SURFBOARD_INTEGRATION.md) — this is a 200 either way, never a 501.',
      properties: {
        status: { type: 'string', enum: ['completed', 'failed', 'skipped'] },
        historyId: { type: 'string', format: 'uuid' },
        externalId: { type: 'string', nullable: true, description: 'The provider\'s merchant id — populated on "completed" or "skipped" (an existing mapping), null on "failed".' },
        errorMessage: { type: 'string', nullable: true },
        message: { type: 'string', nullable: true, description: 'Present on "skipped" — explains why no sync was attempted.' },
      },
    },
    MerchantSyncStatus: {
      type: 'object',
      properties: {
        merchantId: { type: 'string', format: 'uuid' },
        provider: { type: 'string', example: 'surfboard' },
        connected: { type: 'boolean', description: 'Whether an active provider_links mapping exists for this merchant.' },
        externalId: { type: 'string', nullable: true },
        webKybUrl: { type: 'string', nullable: true, description: 'KYB application link generated by a successful Surfboard Create Merchant call.' },
        surfboardMerchantId: { type: 'string', nullable: true, description: 'Surfboard\'s own merchant id (distinct from `merchantId` above, which is GainBox\'s id) — populated once MERCHANT_CREATED is reached via Check Application Status or the Application Merchant Created webhook. Null while onboarding is still in progress.' },
        surfboardStoreId: { type: 'string', nullable: true, description: 'Surfboard\'s own store id — populated alongside surfboardMerchantId once MERCHANT_CREATED is reached.' },
        applicationStatus: {
          type: 'string',
          nullable: true,
          enum: ['APPLICATION_INITIATED', 'APPLICATION_SUBMITTED', 'APPLICATION_PENDING_INFORMATION', 'APPLICATION_SIGNED', 'APPLICATION_COMPLETED', 'MERCHANT_CREATED', 'APPLICATION_REJECTED', 'APPLICATION_EXPIRED'],
          description: 'Raw Surfboard KYB application status, as last confirmed by Check Application Status. Null until the first successful status refresh.',
        },
        paymentMethods: {
          type: 'array',
          nullable: true,
          items: { type: 'object', properties: { paymentMethod: { type: 'string' }, status: { type: 'string' }, enabledSchemes: { type: 'array', items: { type: 'string' } } } },
          description: 'Payment methods Surfboard has activated for this merchant, as of the last status refresh. Null until then.',
        },
        billingPlans: {
          type: 'array',
          nullable: true,
          items: { type: 'object' },
          description: 'Billing plans attached to this merchant\'s application, as of the last status refresh. Null until then.',
        },
        enabledPaymentMethods: {
          type: 'array',
          nullable: true,
          items: { type: 'object', properties: { paymentMethodId: { type: 'string' }, paymentMethod: { type: 'string' } } },
          description: 'Phase 3. Result of Surfboard\'s dedicated List Payment Methods endpoint — the authoritative source for currently-enabled methods, distinct from the KYB-flow `paymentMethods` byproduct above. Null until the first refresh with a known Surfboard merchantId.',
        },
        settlementReports: {
          type: 'array',
          nullable: true,
          items: { type: 'object' },
          description: 'Phase 3. Surfboard\'s Settlement Reports for this merchant, as of the last status refresh. Often an empty array in the sandbox (no settled transactions yet) — null only before the first refresh with a known Surfboard merchantId.',
        },
        paymentStatus: {
          type: 'string',
          enum: ['not_configured', 'incomplete', 'active'],
          description: 'Phase 3. Derived, not stored — \'active\' once MERCHANT_CREATED is reached and at least one payment method is enabled/activated; \'incomplete\' once MERCHANT_CREATED but no method is active yet; \'not_configured\' before that.',
        },
        syncStatus: { type: 'string', enum: ['never_started', 'pending', 'running', 'completed', 'failed', 'skipped'] },
        lastSyncAt: { type: 'string', format: 'date-time', nullable: true },
        lastSyncResult: { type: 'string', nullable: true },
        lastError: { type: 'string', nullable: true },
        pendingEvents: { type: 'integer', description: 'Outbox events queued for this merchant that the worker hasn\'t processed yet.' },
      },
    },
    BranchSyncStatus: {
      type: 'object',
      description: 'Phase 2 — Store & Device Integration. Same shape as MerchantSyncStatus, one level down (Branch -> Surfboard Store).',
      properties: {
        branchId: { type: 'string', format: 'uuid' },
        provider: { type: 'string', example: 'surfboard' },
        connected: { type: 'boolean', description: 'Whether an active provider_links mapping exists for this branch.' },
        surfboardStoreId: { type: 'string', nullable: true },
        storeStatus: { type: 'string', nullable: true, enum: ['ACTIVE', 'DEACTIVATED', 'BLOCKED', 'INACTIVE'], description: 'Surfboard\'s own Store status, as of the last status refresh. Null until then.' },
        onlineOnboardingStatus: { type: 'string', nullable: true, enum: ['APPROVED', 'INITIATED', 'FAILED'] },
        syncStatus: { type: 'string', enum: ['never_started', 'pending', 'running', 'completed', 'failed', 'skipped'] },
        lastSyncAt: { type: 'string', format: 'date-time', nullable: true },
        lastSyncResult: { type: 'string', nullable: true },
        lastError: { type: 'string', nullable: true },
      },
    },
    DeviceSyncStatus: {
      type: 'object',
      description: 'Phase 2 — Store & Device Integration. Same shape as BranchSyncStatus, one level down (Device -> Surfboard Terminal).',
      properties: {
        deviceId: { type: 'string', format: 'uuid' },
        provider: { type: 'string', example: 'surfboard' },
        connected: { type: 'boolean', description: 'Whether an active provider_links mapping exists for this device.' },
        surfboardTerminalId: { type: 'string', nullable: true },
        terminalStatus: { type: 'string', nullable: true, enum: ['REGISTERED', 'ACTIVE', 'IN_ACTIVE', 'DE_REGISTERED'] },
        lastAliveAt: { type: 'string', format: 'date-time', nullable: true, description: 'Last time the terminal successfully communicated with Surfboard.' },
        batteryPercentage: { type: 'number', nullable: true },
        isCharging: { type: 'boolean', nullable: true },
        powerSource: { type: 'string', nullable: true, enum: ['EXTERNAL_POWER', 'BATTERY'] },
        deviceNetwork: { type: 'string', nullable: true, enum: ['WIFI', 'GSM'] },
        terminalType: { type: 'string', nullable: true, example: 'surfpad' },
        serialNo: { type: 'string', nullable: true },
        terminalPaymentMethods: { type: 'array', nullable: true, items: { type: 'string' } },
        syncStatus: { type: 'string', enum: ['never_started', 'pending', 'running', 'completed', 'failed', 'skipped'] },
        lastSyncAt: { type: 'string', format: 'date-time', nullable: true },
        lastSyncResult: { type: 'string', nullable: true },
        lastError: { type: 'string', nullable: true },
      },
    },
    MerchantSyncHistoryEntry: {
      type: 'object',
      properties: {
        id: { type: 'string', format: 'uuid' },
        entityType: { type: 'string', example: 'merchant' },
        entityId: { type: 'string', format: 'uuid' },
        provider: { type: 'string', example: 'surfboard' },
        status: { type: 'string', enum: ['pending', 'running', 'completed', 'failed', 'skipped'] },
        startedAt: { type: 'string', format: 'date-time', nullable: true },
        finishedAt: { type: 'string', format: 'date-time', nullable: true },
        durationMs: { type: 'integer', nullable: true },
        attempts: { type: 'integer' },
        errorMessage: { type: 'string', nullable: true },
        correlationId: { type: 'string', nullable: true },
        triggeredBy: { type: 'string', enum: ['system', 'manual'] },
        actorUserId: { type: 'string', format: 'uuid', nullable: true },
        createdAt: { type: 'string', format: 'date-time' },
        updatedAt: { type: 'string', format: 'date-time' },
      },
    },
  },
}
